global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'mongogate'
    static_configs:
      - targets: ['mongogate:9090']
    metrics_path: /metrics

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files: []
